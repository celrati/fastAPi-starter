__A complete starter fastApi by celr@ti__

* First Step
 Create a new env with conda
 so you need to install conda before
 * conda create --name lenaEnv --file requirements.txt
* start the server : ./start.sh